import Vue from 'vue'

import { Button, Cell, CellGroup, NavBar, Field, Toast, CountDown } from 'vant'

Vue.use(Button)
  .use(Cell)
  .use(CellGroup)
  .use(NavBar) // 导航栏
  .use(Field) // 输入框
  .use(Toast)
  .use(CountDown) // 倒计时
